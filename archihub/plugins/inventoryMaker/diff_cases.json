{
  "cases": [
    {
      "name": "plugins: inventoryMaker bulk needs a role",
      "method": "POST",
      "path": "/inventoryMaker/bulk",
      "body": {},
      "auth": false,
      "expect_status": 401
    },
    {
      "name": "plugins: task results need a role",
      "path": "/inventoryMaker/filedownload/whatever",
      "auth": false,
      "expect_status": 401
    },
    {
      "name": "plugins: the public inventory refuses a request naming no view",
      "method": "POST",
      "path": "/inventoryMaker/public/downloadInventory",
      "body": {
        "post_type": "*"
      },
      "auth": false,
      "expect_status": 400,
      "_note": "Deliberately malformed, so it is refused BEFORE anything is generated - the route is unauthenticated and writes a workbook to the web files volume when it succeeds. The rule NOT covered here is that the export applies access rights, so a reserved resource stays out of a public inventory; verifying that needs an instance with a reserved resource in a public view, and tests/test_plugin_framework.py covers the rule itself."
    },
    {
      "name": "plugins: a plugin declaring no settings is 404, not a traceback",
      "path": "/inventoryMaker/settings/settings",
      "auth": true,
      "expect_status": 404,
      "_note": "Legacy answers 500 here for both inventoryMaker and massiveUpdater, which declare no settings block."
    },
    {
      "name": "plugins: the inventoryMaker icon needs a token",
      "path": "/inventoryMaker/image",
      "auth": false,
      "expect_status": 401
    }
  ]
}
